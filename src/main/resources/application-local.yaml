spring:
  application:
    name: incident-tracker

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/incident_tracker
    username: incident_user
    password: Incident_2026!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true




logging:
  level:
    root: INFO                        # Le reste reste propre
    com.incident.tracker: DEBUG        # Votre code métier
    org.springframework.web: DEBUG    # Détails des requêtes HTTP
    org.hibernate.SQL: DEBUG          # Pour voir les requêtes SQL

