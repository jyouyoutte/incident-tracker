spring:
  application:
    name: incident-tracker

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/incident_tracker
    username: incident_user
    password: Incident_2026!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true




logging:
  level:
    root: INFO                        # Keep the rest clean
    com.incident.tracker: DEBUG       # Your business code
    org.springframework.web: DEBUG    # HTTP request details
    org.hibernate.SQL: DEBUG          # To see SQL queries

